# This is the settings/configuration file of the 'run_hgs_restart.sb' script
# variable definitions in this file take precedence over default values that
# are defined in teh caller script.
# the default name of this script is 'hgs_settings.sh' and 'run_hgs_restart.sb'
# will look for this script in its working directory first and then in the
# $HOME directory of the user.
# It is primarily intended to specify the path of the executables.
GROKEXE="$HOME/hgs_bin/grok.x"
HGSEXE="$HOME/hgs_bin/hgs.x"
HSPEXE="$HOME/hgs_bin/hsplot.x"
